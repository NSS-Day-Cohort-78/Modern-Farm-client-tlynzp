// Define and export a function for creating a seed named createSoybean (using arrow functions)

// The seed contains the following properties:
// type
// height
// output

export const createSoybean = () => {
    const soybeanObject = {
        type: `Soybean`,
        height: 20,
        output: 4,
    }

    return soybeanObject
}